apiVersion: v1
kind: Service
metadata:
  name: {{ include "workout-timer.fullname" . }}
  labels:
    {{- include "workout-timer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workout-timer.selectorLabels" . | nindent 4 }}
